A+ Studio 1.0.0 (6) finalization trigger.

Purpose: create a fresh Publisher deploy run after the post-deploy finalizer workflow already exists on main. The finalizer runs only after the Publisher deploy completes successfully, recovers any orphaned Google Play upload for Build 6, verifies Production versionCode 6, and then safely attaches/submits Apple Build 6.
